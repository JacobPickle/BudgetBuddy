<p>
    <%= item.name %>: $<%= item.price %>

    <%= link_to "Delete", item, data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</p>