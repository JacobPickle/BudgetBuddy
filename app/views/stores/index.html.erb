<%= render "/purchases/sidebar" %>

<div class="main">
  <h1>Select Store</h1>

  <% @stores.each do |store| %>
    <%= render store %>
  <% end %>
  </br>

  <%= link_to "New Store", new_store_path(@store) %>
</div>
